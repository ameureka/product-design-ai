{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "github": {
    "silent": true
  },
  "env": {
    "NEXT_PUBLIC_APP_URL": "${VERCEL_URL}"
  }
} 